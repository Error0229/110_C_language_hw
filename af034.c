#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int end(char* k)
{
    return strlen(k);
}
int main()
{
    char input[150], ins[150];
    memset(input, 0, sizeof(input));
    memset(ins, 0, sizeof(ins));
    gets(input);
    gets(ins);
    int i = 0, j = 0;
}